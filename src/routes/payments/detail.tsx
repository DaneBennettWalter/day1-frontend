/**
 * /payments/:id — detail view delegates to feature component.
 */

import { PaymentDetail } from '@/features/payments/components/PaymentDetail'

export default function PaymentDetailRoute() {
  return <PaymentDetail />
}
